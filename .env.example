# production = MongoDB | development = data/example.json
# Defaults to development in dev, production when deployed if unset
DATA_SOURCE=development

# Required when DATA_SOURCE=production
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=tinyExpense
